~ @page.to_html(self)

/%h1 Medicine

/%section.articles= article_summaries(latest_articles(20))

/= articles_heading

/ - Nesta::Page.find_all.each do |page|
/   %a{ :href => page.abspath }
/     #{page.heading}

