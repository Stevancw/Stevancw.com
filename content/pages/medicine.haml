%h1 Medicine


%section.articles= pages

%h3 This isn't working right now. I'll fix it soon (probably)...

/= Nesta::Page.find_all.select { |p| p.flagged_as?('wow') }

/= articles_heading

/ - Nesta::Page.find_all.each do |page|
/   %a{ :href => page.abspath }
/     #{page.heading}

